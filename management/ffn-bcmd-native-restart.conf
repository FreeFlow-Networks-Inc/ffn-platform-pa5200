[Unit]
# The retained unit originally required an OpenWrt compatibility mount. The
# native Debian BCM launcher uses the local executable and local configuration.
ConditionPathIsMountPoint=
ConditionPathExists=/usr/local/ffn/bcm.user
ConditionPathExists=/usr/share/broadcom/config.bcm
